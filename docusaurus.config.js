// @ts-check

/** @type {import('@docusaurus/types').Config} */
const config = {
  title: "Example",
  url: "https://example.com",
  baseUrl: "/",
  presets: [["classic", {}]],
};

module.exports = config;
